name: flexible
description: Flutter UI scalable layout scheme. We want to be able to adapt perfectly to different
  resolution devices. Scales to the width of the screen.
version: 0.1.4
author: songfei <songfei.org@gmail.com>
homepage: https://github.com/songfei/flutter_flexible

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  test:
  flutter_test:
    sdk: flutter

flutter: